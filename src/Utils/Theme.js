import { createTheme } from "@mui/material/styles";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#FF385C",
      black: "#000000",
      white: "#FFFFFF",
    },
  },
});
